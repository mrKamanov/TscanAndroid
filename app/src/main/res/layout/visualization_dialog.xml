<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/background">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Визуализация обработки"
        android:textSize="18sp"
        android:textColor="@color/text_inverse"
        android:fontFamily="@font/krabuler"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Найденный контур (зеленая рамка):"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:fontFamily="@font/krabuler"
        android:layout_marginBottom="8dp" />

    <ImageView
        android:id="@+id/iv_contour_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="fitCenter"
        android:background="@drawable/edit_text_background"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сетка обработки (красные линии):"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:fontFamily="@font/krabuler"
        android:layout_marginBottom="8dp" />

    <ImageView
        android:id="@+id/iv_grid_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="fitCenter"
        android:background="@drawable/edit_text_background"
        android:layout_marginBottom="16dp" />

    <!-- ========== КНОПКА ЗАКРЫТЬ В ЕДИНОМ ОВАЛЬНОМ СТИЛЕ ========== -->
    <!-- Кнопка Закрыть (серая овальная как в шаблоне) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_close"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="8dp"
        android:text="Закрыть"
        android:textColor="#CBD5E1"
        android:textStyle="bold"
        android:textSize="16sp"
        android:fontFamily="@font/krabuler"
        android:backgroundTint="#1E293B"
        app:cornerRadius="28dp"
        app:strokeColor="#475569"
        app:strokeWidth="2dp"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:elevation="0dp"
        android:stateListAnimator="@null"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:contentDescription="Закрыть окно визуализации обработки" />

</LinearLayout>
